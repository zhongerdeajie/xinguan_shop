feat: 优惠券/地址流水 + 库存预留超时释放 + 审计查询 + 前端评价

1. user_coupon_log 与 address_log 写入逻辑
   - models.go 新增 UserCouponLog/AddressLog model(只追加流水)
   - MarkUserCouponUsed 核销时写 user_coupon_log(USED)
   - Create/Update/DeleteAddressBook 写 address_log(完整快照JSON)

2. inventory_reservation 超时释放
   - outbox-worker 加 releaseExpiredReservations(每2分钟)
   - 释放下单30分钟未支付的库存占用: 回补MySQL+Redis, 预留置status=2
   - 用事务+幂等(只更新status=1)

3. 管理端审计查询(audit包)
   - GET /api/v1/admin/audit/refunds  退款流水(按orderId/userId过滤)
   - GET /api/v1/admin/audit/stock   库存审计(按dishId过滤)

4. NestJS users.service: findAll/findOne 加 deletedAt:null 软删过滤

5. 前端评价功能
   - lib/api.ts 加 reviewsAPI(走nginx /go代理到go-service)
   - types 加 DishReview 类型
   - HomeClient 菜品详情弹窗内嵌评价展示+提交(星级/文字/匿名)
   - DishCard 加 onReview 支持

编译: go build/vet, nestjs build, 前端 typecheck, 集成测试 全部通过
注: Docker 环境磁盘满(53GB vhdx), next-web 镜像构建报 read-only, 是环境问题非代码问题
